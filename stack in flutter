
import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false ,
      home: Scaffold(
          appBar: AppBar(
            centerTitle:true,
            title: Text('Hello'),
            backgroundColor: Colors.blue,
          ), //AppBar
          body: Center(
            child: SizedBox(
              width: 100,
              height: 100,
              child: Center(
                child: Stack(
                  children: <Widget>[
                    Container(
                      width: 100,
                      height: 100,
                      color: Colors.red,
                    ), //Container
                    Container(
                      width: 90,
                      height: 90,
                      color: Colors.green,
                    ), //Container
                    Container(
                      height: 80,
                      width: 80,
                      color: Colors.blue,
                    ), //Container
                  ], //<Widget>[]
                ), //Stack
              ), //Center
            ), //SizedBox
          ) //Center
      ) //Scaffold
  ) //MaterilApp
  );
}
